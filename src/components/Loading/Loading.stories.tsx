import React from 'react';
import { Loading } from './Loading';

const config = {
  title: 'Loading',
  component: Loading
};

export default config;

export const LoadingSB: React.FC = () => <Loading />;
